# Neon Postgres (server-side only - used in API routes for claim persistence)
# Provisioned automatically by the Vercel ↔ Neon Marketplace integration.
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/dbname?sslmode=require

# Resend (email delivery)
RESEND_API_KEY=your-resend-api-key
